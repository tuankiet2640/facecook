# Vite dev server proxies /api → gateway and /ws → chat-service.
# These values are only used for local development outside Docker.
VITE_API_BASE=http://localhost:8080
VITE_WS_BASE=ws://localhost:8084
